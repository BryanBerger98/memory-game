module.exports = {
    indexPlusOne: (i) => {
        return i+1;
    },
    isFirst: (i) => {
        return +i === 0 ? true : false;
    },
    isSecond: (i) => {
        return +i === 1 ? true : false;
    },
    isThird: (i) => {
        return +i === 2 ? true : false;
    }
}